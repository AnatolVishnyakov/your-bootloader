apiVersion: 1

providers:
  - name: 'Release boards'
    orgId: 1
    folder: 'Dashboards'
    type: file
    allowUiUpdates: true
    updateIntervalSeconds: 10
    options:
      path: /etc/grafana/provisioning/dashboards/json